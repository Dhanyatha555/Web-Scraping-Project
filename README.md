# Web Scraping Project

This Python-based web scraping tool extracts structured data from websites using `requests` and `BeautifulSoup` libraries.

## Features
- Sends HTTP requests to retrieve webpage content
- Parses HTML data and extracts useful information
- Displays the scraped content in a clean format

## Requirements
```bash
pip install requests
pip install beautifulsoup4
